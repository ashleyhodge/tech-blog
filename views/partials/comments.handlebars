<div class="comments">
  {{#each this}}
  <section class="comment mb-3">
    <div class="meta mb-1">
      <p class="fw-bold">{{user.username}} on {{format_date created_at}} said:</p>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>